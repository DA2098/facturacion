# ═══════════════════════════════════════════════════════════════
# .env — Variables de entorno del backend FACTS
# ═══════════════════════════════════════════════════════════════
# Copiar como .env y ajustar los valores:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════

PORT=3001

# PostgreSQL (connection string or individual vars)
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=facts
DB_USER=postgres
DB_PASSWORD=password

# JWT Secret
JWT_SECRET=faktsdev_2025_jwt_secure_key_xyz789
JWT_EXPIRES=24h
